package com.project.kakao_login.presentation.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.ui.graphics.Shape
import androidx.compose.ui.unit.dp

// Wear OS에서 사용할 수 있는 사용자 정의 Shape들
val SmallShape: Shape = RoundedCornerShape(4.dp)
val MediumShape: Shape = RoundedCornerShape(8.dp)
val LargeShape: Shape = RoundedCornerShape(0.dp)

// 필요에 따라 더 많은 사용자 정의 Shape을 추가할 수 있습니다.
val FullRoundedShape: Shape = RoundedCornerShape(50)